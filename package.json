{
  "name": "swg-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Southern Writers Guild — static multi-page site. Vite builds js/sanity.js (which uses @sanity/client) and every page's scripts into dist/; api/ functions are separate, unbundled Vercel serverless functions.",
  "scripts": {
    "build": "vite build && vite build --config vite.bootstrap.config.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "@sanity/client": "^8.4.0",
    "@sanity/preview-url-secret": "^4.1.5",
    "@sanity/visual-editing": "^6.1.1"
  },
  "devDependencies": {
    "vite": "^8.2.2",
    "vite-plugin-static-copy": "^4.1.1"
  }
}
